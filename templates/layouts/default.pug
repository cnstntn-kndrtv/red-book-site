include ../mixins/flash-messages

doctype html
html

    //- HTML HEADER
    head
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")

        title= (title) ? title + ' - Красная книга русского языка' : 'Красная книга русского языка'
        include ../mixins/favicon

        //- Customise the stylesheet for your site by editing /public/styles/site.sass
        link(href="/styles/site.css", rel="stylesheet")

        //- This file provides the default styling for the KeystoneJS Content Editor
        if user && user.canAccessKeystone
            link(href="/keystone/styles/content/editor.min.css", rel="stylesheet")

        //- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries
        //- Safely remove this block if you are not targeting IE8
        //-[if lt IE 9]
            script(src="//cdn.jsdelivr.net/html5shiv/3.7.3/html5shiv.js")
            script(src="//cdn.jsdelivr.net/respond/1.4.2/respond.min.js")
        //-[endif]

        //- Include template-specific stylesheets by extending the css block
        block css

        //- Add any other template-specific HEAD tags by extending the head block
        block head

    //- HTML BODY
    body.section-nude

        //- HEADER
        #header

            //- Customise your site's navigation by changing the navLinks Array in ./routes/middleware.js
            //- ... or completely change this header to suit your design.
                        
            nav.navbar.navbar-default(role='navigation-demo')
                .container
                    .navbar-header
                        button.navbar-toggle(type='button', data-toggle='collapse', data-target='#navigation')
                            span.sr-only Toggle navigation
                            span.icon-bar
                            span.icon-bar
                            span.icon-bar
                        a.navbar-brand(href='/')
                            img(alt='ККРЯ' src='/images/logo.png')
                    
                    #navigation.collapse.navbar-collapse
                        ul.nav.navbar-nav
                            each link in navLinks
                                li(class=(section == link.key ? 'active' : null)): a.btn(href=link.href class=(link.style ? link.style : 'btn-simple'))= link.label
                        
                        ul.nav.navbar-nav.navbar-right
                            li
                                a(href="https://play.google.com/store/apps/details?id=one.kloud.rusbook" target="_blank").btn.btn-simple.social
                                    img(src="images/googleplay-square.png", alt="google play")
                            li
                                a(href="https://vk.com/rusredbook" target="_blank").btn.btn-simple.social
                                    img(src="images/vk.png", alt="vkontakte")
                            li
                                a(href="https://www.facebook.com/rusredbook/" target="_blank").btn.btn-simple.social
                                    img(src="images/fb.png", alt="facebook")
        //- BODY
        #body
            //- NOTE:
            //- There is no .container wrapping class around body blocks to allow more flexibility in design.
            //- Remember to include it in your templates when you override the intro and content blocks!

            //- The Intro block appears above flash messages (used for temporary information display)
            block intro

            //- Flash messages allow you to display once-off status messages to users, e.g. form
            //- validation errors, success messages, etc.
            +flash-messages(messages)

            //- The content block should contain the body of your template's content
            block content


        //- FOOTER
        footer.container-fluid.section-light-brown: #footer
            .container
                .row
                    .regalia.col-lg-4.col-md-4.col-sm-4.col-xs-12
                        img(src="images/marsu.png").logo
                        .content Сайт создан лабораторией 'AI Labs'
                            br
                            a(href="https://marsu.ru" target='_blank').text-danger Марийского государственного университета
                    .regalia.col-lg-4.col-md-4.col-sm-4.col-xs-12
                        img(src="images/minobr.png").logo
                        .content При поддержке Министерства Образования и Науки Российской Федерации
                    
                    .col-lg-4.col-md-4.col-sm-4.col-xs-12
                        ul.nav.navbar-nav
                            li
                                a(href="https://play.google.com/store/apps/details?id=one.kloud.rusbook" target="_blank").btn.btn-simple.social
                                    img(src="images/googleplay-square.png", alt="google play")
                                    span Google Play
                            li
                                a(href="https://vk.com/rusredbook" target="_blank").btn.btn-simple.social
                                    img(src="images/vk.png", alt="vkontakte")
                                    span Вконтакте
                            li
                                a(href="https://www.facebook.com/rusredbook/" target="_blank").btn.btn-simple.social
                                    img(src="images/fb.png", alt="facebook")
                                    span Facebook
                            li
                                a(href='mailto:dev@digt.ru?subject=Сайт Красная книга русского языка').btn.btn-simple.social
                                    img(src="images/email.png", alt="facebook")
                                    span dev@digt.ru
                            //- if user
                            //-     if user.canAccessKeystone
                            //-         li: a(href='/keystone').btn.btn-danger Открыть панель управления
                            //-     li: a(href='/keystone/signout').btn.btn-danger Выйти
                            //- else
                            //-     li: a(href='/keystone/signin').btn.btn-danger Войти




        //- JAVASCRIPT

        //- jQuery 1.11.3 is included by default, and required by both Bootstrap
        //- and the KeystoneJS Content Editor. If you are not targeting IE8,
        //- you can upgrade to 2.1.4 (also bundled) for better performance.
        script(src='/js/jquery/jquery-1.11.3.min.js')

        //- Customise which Bootstrap javascript files are served by including
        //- them specifically here, instead of bootstrap-3.3.5.min.js
        script(src='/js/bootstrap/bootstrap-3.3.5.min.js')
        script(src="/js/ct-paper.js")
        //- The KeystoneJS Content Editor provides support for ks-editable data attributes,
        //- which generate links to edit content for users who can access Keystone
        if user && user.canAccessKeystone
            script(src='/keystone/js/content/editor.js')

        //- Add scripts that are globally required by your site here.

        //- google analytics
        script(async='', src='https://www.googletagmanager.com/gtag/js?id=UA-8058363-20')
        script.
            window.dataLayer = window.dataLayer || [];
            function gtag(){ dataLayer.push(arguments); }
            gtag('js', new Date());
            gtag('config', 'UA-8058363-20');

        script.
            function clickToImageVideo(item) {
                var plugVideo = document.getElementById(item.id);

                var iframeVideo = document.createElement("iframe");
                iframeVideo.width ="229px";
                iframeVideo.height ="153px";
                iframeVideo.src= item.id;
                iframeVideo.frameborder="0";
                iframeVideo.setAttribute("allowFullScreen", "");
                iframeVideo.setAttribute("max-height", "153px;");

                var parent = plugVideo.parentNode;
                while (parent.firstChild) { 
                    parent.removeChild(parent.firstChild); 
                }
                parent.appendChild(iframeVideo);
            }

        //- yandex metrics
        script(type='text/javascript').
            (function (d, w, c) {
                try {
                    w.yaCounter46215879 = new Ya.Metrika({
                        id:46215879,
                        clickmap:true,
                        trackLinks:true,
                        accurateTrackBounce:true
                    });
                } catch(e) { }
            
                var n = d.getElementsByTagName("script")[0],
                s = d.createElement("script"),
                f = function () { n.parentNode.insertBefore(s, n); };
                s.type = "text/javascript";
                s.async = true;
                s.src = "https://mc.yandex.ru/metrika/watch.js";
                if (w.opera == "[object Opera]") {
                    d.addEventListener("DOMContentLoaded", f, false);
                } else { f(); }
            })(document, window, "yandex_metrika_callbacks");
        
        noscript
            div
                img(src='https://mc.yandex.ru/watch/46215879', style='position:absolute; left:-9999px;', alt='')

        
        //- Include template-specific javascript files by extending the js block
        block js
