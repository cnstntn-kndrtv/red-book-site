extends ../layouts/default

block content
    .container
        h2 Участники проекта
        .row(style='margin-bottom: 32px; margin-top: 32px;')
            for item in video
                .col-md-4
                    iframe(width='100%', height='100%', src=item, frameborder='0', allowfullscreen)